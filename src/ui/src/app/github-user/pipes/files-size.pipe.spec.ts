import { FilesSizePipe } from './files-size.pipe';

describe('FilesSizePipe', () => {
  it('create an instance', () => {
    const pipe = new FilesSizePipe();
    expect(pipe).toBeTruthy();
  });
});
